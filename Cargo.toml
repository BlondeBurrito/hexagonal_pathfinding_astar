[package]
name = "bevy_pathfinding_astar"
version = "0.1.0"
authors = ["BlondeBurrito"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = { version = "0.5", default-features = false, features = ["dynamic"] }
